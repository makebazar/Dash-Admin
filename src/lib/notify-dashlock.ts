/**
 * Fires a webhook to DashLock telling it to re-pull products from DashAdmin.
 * DashLock integration has been disabled/deleted.
 */
export async function notifyDashLockProductsUpdated(clubId: number): Promise<void> {
    // Integration disabled
}
